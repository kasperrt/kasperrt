apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: static-sites
  namespace: static-sites
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - secretName: static-sites-tls
      hosts:
        - kasperrt.me
        - www.kasperrt.me
  rules:
    - host: kasperrt.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: static-sites
                port:
                  number: 80
    - host: www.kasperrt.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: static-sites
                port:
                  number: 80
